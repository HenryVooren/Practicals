BoxLayout:
    orientation: 'vertical'
    Button:
        text: 'Clear'
        on_press: app.clear_fields(self)
    TextInput:
        id: input_name
        text: ''
    Button:
        text: 'Greet'
        on_press: app.handle_greet(self)
    Label:
        id: my_label
        color: (1, 1, 0, 1)